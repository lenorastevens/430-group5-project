const SearchBar = () => {
  return (
    <div>
        <input
          type="text"
          placeholder="Search..."
          className="search-bar"
        />
    </div>
  );
};

export default SearchBar;
